# WS5 (security) dev/test deps.
#
# P3-7 reconciled these into the root requirements.txt. Runtime deps: httpx
# (Model Armor REST), pydantic (via contracts). Test-only: pytest, pytest-asyncio.
#
# From the repo root: pip install -r requirements.txt
