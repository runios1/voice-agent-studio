# P2-6 — auto-pause / escalation engine
#
# P3-7 reconciled these into the root requirements.txt. Runtime dep: pydantic
# (the frozen event contract, contracts/events, is pydantic v2). Dev/CI: pytest.
#
# From the repo root: pip install -r requirements.txt
