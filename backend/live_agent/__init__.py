"""backend/live_agent — the Live-native conversational agent (Phase 4).

This package implements the frozen `contracts/live_agent` interfaces. This
workstream (P4-3) provides `moderation.py`: the `StreamModerator` that screens
Live's output transcription as the net around the agent's speech.
"""

from __future__ import annotations
