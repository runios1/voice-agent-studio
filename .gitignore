# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node / frontend
node_modules/
dist/
build/
.vite/
*.tsbuildinfo

# Env & secrets — NEVER commit. Also never let these reach a model context.
.env
.env.*
!.env.example
*.key
credentials.json
service-account*.json

# Local SQLite persistence (config/orchestrator/events/auth) — the zero-config
# dev default when DATABASE_URL is unset.
.data/
*.db
*.db-wal
*.db-shm

# OS / editor
.DS_Store
.idea/
.vscode/
